name: archmage_rts
description: "Archmage RTS"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flame: ^1.30.1
  flutter:
    sdk: flutter
  provider: ^6.1.5
  state_view: ^0.1.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
